# PostgreSQL
#   Ensure the PostgreSQL gem is defined in your Gemfile
#   gem 'pg'
development:
  adapter: postgresql
  encoding: utf8
  database: nowatt_sample_development
  username: mparramon
  host: localhost
  pool: 5
  port: 5432
  
